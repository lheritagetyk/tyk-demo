extend type Query {
    getCustomers(pagination: Pagination): Customers!
    getCustomerInfo: Customer!
    getCustomer(customerId: ID): Customer
}

type Customers implements PaginatedArray {
    page: PageMetadata
    customers: Customer
}